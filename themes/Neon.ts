import { createTheme } from '@css/theme.config'

export const Neon = createTheme({
  colors: {
    background: '#000',
    text: '#ECEFF4',
    inactive: '#383838',
    inactiveBg: '#1C1C1C',
    buttonBg: 'transparent',
    buttonBorder: '#36F213',
    buttonText: '#ECEFF4',
  },
})
